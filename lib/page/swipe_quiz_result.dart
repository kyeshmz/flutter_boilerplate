import 'package:flutter/material.dart';

class SwipeQuizResult extends StatelessWidget {
  const SwipeQuizResult({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
